const config = {
  development: {
    username: 'postgres',
    password: '12345',
    database: 'movie',
    host: 'localhost',
    dialect: 'postgres',
    logging: false,
  }
}

module.exports = config